# Polystrukt Environment Configuration
# Generate a secure random string for JWT_SECRET
JWT_SECRET=

# Optional: Port configuration (defaults to 3000)
# PORT=3000

# Firebase configuration is loaded from firebase-applet-config.json
